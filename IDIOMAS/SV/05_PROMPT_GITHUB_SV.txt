KORRIGERA ENDAST DE 20 SVENSKA SIDORNA I GITHUB-REPOSITORYT `RSBRENTAL/R`, BRANCH `main`, MAPP `IDIOMAS/SV/`.

- Den enda kodkällan för den här uppgiften är `RSBRENTAL/R` → `main` → `IDIOMAS/SV/PAGINAS_REPO/`.
- Exclusive workspace: `IDIOMAS/SV/`. Do not inspect, modify, or use `IDIOMAS/*` from any other language except to verify a cross-language invariant explicitly required by the listed correction.
KÄLLA OCH OMFATTNING
- Läs ALLA filer i `IDIOMAS/SV/` från början till slut innan du ändrar något. Använd inte en annan språkmapp som ersättning.
- Kodkällan är `RSBRENTAL/R` → `main` → `IDIOMAS/SV/PAGINAS_REPO/`.
- Din enda tillåtna omfattning är de 20 sökvägarna i `IDIOMAS/SV/04_MANIFEST_20_PAGINAS_SHA256.csv`.
- Ändra inga sidor på andra språk och inga gemensamma rotfiler, CSS, JS, JSON, XML, fysiska bilder eller konfiguration.

KORRIGERINGAR
1. Läs hela `IDIOMAS/SV/01_CORRECCIONES_POR_PAGINA.md`.
2. Tillämpa ALLA rader i `IDIOMAS/SV/02_ERRORES_DIMENSIONES_IMAGENES.csv`.
3. För varje bildincident ska endast `width` och `height` på berörd `<img>` ändras så att de matchar de verifierade fysiska måtten.
4. Ändra INTE `src`, `srcset`, `sizes`, filnamn, format, komprimering eller de fysiska bildfilerna. Den forensiska granskningen hittade inget generellt fel i `srcset`-bredddeskriptorerna.
5. RSB-003 / Google Maps: ägaren bekräftar att API-nyckeln är begränsad. Ändra eller ta inte bort nyckeln enbart för att den är synlig på klientsidan.
6. Recensioner: det kanoniska värdet är 226. Ändra inte till 222.
- Bokningsupplägget 30 % + 70 % FINNS INTE och är annullerat. Använd det inte, korrigera ingen sida mot det och behandla det inte som officiell information. Depositionen på 250 € kommer från en kundrecension och är INTE en officiell RSB-policy; använd den inte som kanonisk uppgift och lägg inte in den på webbplatsen.
- Regel för kundrecensioner: kunders berättelser är INTE officiella RSB-policyer eller företagsfakta. Gör inte om uppgifter i recensioner om depositioner, belopp, strandbesök, handdukar, badkläder eller liknande till webbplatsinnehåll om de inte bekräftas av en oberoende officiell RSB-källa.
8. Ändra inte canonical, hreflang, x-default, URL:er, Open Graph URL, JSON-LD URL, breadcrumbs, titlar, descriptions, länkar, datum eller orelaterat innehåll om inte en uttryckligen listad korrigering kräver det.
9. Ingen refaktorering, redesign eller valfria förbättringar.

OBLIGATORISK VALIDERING
- Exakt 20 svenska HTML-filer ska finnas kvar.
- Kontrollera alla 20 sökvägar en och en.
- 0 kvarvarande listade `width/height`-förhållandefel.
- 0 nya trasiga bildreferenser.
- 0 ändringar i canonical/hreflang/x-default jämfört med originalet.
- Värdet 226 ska finnas kvar där det redan finns.
- Ingen fil utanför de 20 tillåtna sökvägarna har ändrats.

OBLIGATORISKT GITHUB-FLÖDE
1. Utgå från aktuell `main` och skapa/använd branchen `correcciones/sv`. Gör ALDRIG korrigeringarna direkt på `main`.
2. Ändra endast de 20 godkända svenska HTML-filerna i denna branch.
3. Commit de korrigerade HTML-filerna med ett tydligt meddelande, t.ex. `Correcciones SV: auditoría RSB`.
4. Öppna en Pull Request från `correcciones/sv` till `main`. MERGA INTE. Ägaren granskar och beslutar.
5. PR-beskrivningen ska innehålla: exakt ändrade filer, utförda korrigeringar, valideringsresultat, eventuella återstående punkter och bekräftelse att inget utanför omfattningen ändrats.
6. Skapa en unified diff/patch mot `main` med namnet `DIFF_SV.patch`.

SLUTLEVERANS
- Skapa `RSB_SV_20_HTML_KORRIGERADE.zip` som nedladdningsbar backup. Commit INTE ZIP-filen till repositoryt.
- ZIP-filen ska innehålla:
  - de 20 korrigerade HTML-filerna med exakt deras ursprungliga repo-sökvägar;
  - `ANDRINGSRAPPORT_SV.txt`;
  - `SHA256_SV_FORE_EFTER.csv`;
  - `DIFF_SV.patch` med de exakta ändringarna jämfört med `main`;
  - `GITHUB_PR_SV.txt` med repository, branch, commit-SHA och Pull Request-URL.
- I slutet av chatten ska du ge: ZIP-nedladdningslänk, Pull Request-URL, branchnamn och commit-SHA.
- Läs om denna prompt före leverans och verifiera att ingenting utanför omfattningen har ändrats.
